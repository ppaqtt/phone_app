# 清笺 APP 设计说明书

---

## 一、软件概述

### 1.1 软件名称
- **全称**：清笺笔记应用 V1.0
- **简称**：清笺

### 1.2 软件简介
清笺是一款面向移动终端的个人笔记管理应用，旨在为用户提供简洁、高效的笔记记录和管理体验。应用采用 Material Design 3 设计风格，支持富文本编辑、分类管理、待办任务、提醒通知等功能，帮助用户高效组织和管理个人信息。

### 1.3 运行环境
- **操作系统**：Android 8.0 (API 26) 及以上
- **硬件要求**：支持触摸操作的移动设备，建议屏幕分辨率 720p 及以上

---

## 二、功能结构

### 2.1 功能模块总览

| 模块 | 功能描述 | 优先级 |
|------|----------|--------|
| 笔记管理 | 创建、编辑、删除、查看笔记 | 核心 |
| 分类管理 | 笔记分类创建、编辑、删除、层级结构 | 核心 |
| 待办任务 | 待办清单管理、完成状态切换 | 核心 |
| 搜索功能 | 全文搜索、搜索历史 | 核心 |
| 回收站 | 笔记软删除、恢复、清空 | 核心 |
| 提醒功能 | 笔记/待办提醒设置、重复提醒 | 重要 |
| 富文本编辑 | 文字样式、列表、表格、图片、音频 | 重要 |
| OCR识别 | 图片文字提取 | 重要 |
| 语音转文字 | 语音输入识别 | 重要 |
| 数据导出 | PDF导出、长图导出 | 重要 |
| 数据备份 | 本地备份、恢复 | 重要 |
| 应用安全 | 应用锁、生物识别 | 重要 |
| 主题定制 | 颜色主题、深色模式 | 次要 |
| 统计功能 | 笔记统计、字数统计 | 次要 |
| 桌面小部件 | 笔记小部件、快捷方式 | 次要 |

### 2.2 核心功能模块详细设计

#### 2.2.1 笔记管理模块

**功能说明**：提供笔记的全生命周期管理，包括创建、编辑、查看、删除等操作。

**功能特性**：
- 新建笔记：点击浮动按钮快速创建
- 编辑笔记：支持标题、内容编辑
- 笔记列表：按分类筛选、多种排序方式
- 笔记卡片：显示标题、内容预览、分类、标签、优先级
- 置顶功能：重要笔记置顶显示
- 删除功能：支持撤销删除（5秒内）

**数据结构**（核心字段）：
- `id`：笔记唯一标识
- `title`：标题
- `content`：内容（支持Markdown格式）
- `categoryId`：所属分类ID
- `tags`：标签（逗号分隔）
- `isPinned`：是否置顶
- `priority`：优先级（0-普通、1-重要、2-紧急）
- `color`：卡片颜色
- `reminderTime`：提醒时间
- `reminderRepeat`：重复模式（NONE/DAILY/WEEKLY/MONTHLY/YEARLY）
- `createdAt`：创建时间
- `updatedAt`：更新时间
- `deletedAt`：删除时间（软删除标记）

#### 2.2.2 分类管理模块

**功能说明**：支持笔记的分类组织，支持层级结构。

**功能特性**：
- 创建分类：自定义名称和颜色
- 分类列表：层级缩进显示
- 分类筛选：在笔记列表按分类筛选
- 移动笔记：将笔记移动到其他分类
- 删除分类：级联清理关联笔记的分类信息

**数据结构**（核心字段）：
- `id`：分类唯一标识
- `name`：分类名称
- `color`：分类颜色
- `parentId`：父分类ID（支持层级）
- `createdAt`：创建时间

#### 2.2.3 待办任务模块

**功能说明**：独立的待办任务管理功能，支持任务创建、完成状态切换、提醒等。

**功能特性**：
- 新建待办：设置标题、内容、优先级、提醒
- 状态切换：标记完成/未完成
- 列表展示：区分待完成和已完成区域
- 批量操作：清除已完成任务

**数据结构**（核心字段）：
- `id`：任务唯一标识
- `title`：任务标题
- `content`：任务描述
- `priority`：优先级（0-普通、1-重要、2-紧急）
- `isCompleted`：完成状态
- `reminderTime`：提醒时间
- `createdAt`：创建时间

#### 2.2.4 搜索功能模块

**功能说明**：支持对笔记内容、标题、标签的全文搜索。

**功能特性**：
- 实时搜索：输入时实时匹配
- 搜索历史：保存搜索记录
- 结果展示：笔记卡片列表展示

#### 2.2.5 回收站模块

**功能说明**：笔记删除后进入回收站，支持恢复或永久删除。

**功能特性**：
- 软删除：删除的笔记保留30天
- 恢复功能：从回收站恢复笔记
- 清空回收站：永久删除所有已删除笔记
- 自动清理：30天后自动清理过期笔记

#### 2.2.6 富文本编辑模块

**功能说明**：提供丰富的文本编辑功能，支持多种格式。

**功能特性**：
- 文字样式：加粗、斜体、下划线、删除线、高亮
- 文字格式：字号调整、颜色设置
- 对齐方式：左对齐、居中、右对齐
- 列表功能：有序列表、无序列表
- 待办列表：可勾选的待办项
- 表格功能：插入和编辑表格
- 图片支持：多图选择、拍照、涂鸦
- 音频支持：添加音频文件

#### 2.2.7 提醒功能模块

**功能说明**：为笔记和待办设置提醒通知。

**功能特性**：
- 设置提醒时间
- 重复提醒：每天、每周、每月、每年
- 通知推送：系统通知提醒
- 提醒管理：取消已设置的提醒

---

## 三、界面设计

### 3.1 主界面（笔记列表页）

**布局结构**：
- 顶部导航栏：标题、搜索、分类、设置、更多菜单
- 分类筛选栏：横向滚动的分类标签
- 笔记列表区域：垂直滚动的笔记卡片列表
- 浮动操作按钮：新建笔记

**交互设计**：
- 点击笔记卡片：进入编辑页面
- 点击分类标签：筛选对应分类的笔记
- 长按/更多操作：显示操作菜单（置顶、标签、删除、移动、优先级、分享）

### 3.2 笔记编辑页面

**布局结构**：
- 顶部导航栏：返回、查找、撤销、重做、保存、更多（导出）
- 标题输入区：带提示文字的标题输入框
- 元信息区：创建时间、字数统计、分类选择
- 标签区：标签展示和编辑
- 提醒区：提醒时间和重复模式设置
- 内容编辑区：富文本编辑区域
- 底部工具栏：文字样式、列表、待办、图片、语音、更多工具

**交互设计**：
- 自动保存：编辑过程中自动保存草稿
- 退出确认：有未保存修改时弹出确认对话框
- 查找功能：笔记内搜索文字

### 3.3 待办任务页面

**布局结构**：
- 顶部导航栏：标题、返回、清除已完成
- 待完成区域：未完成任务列表
- 已完成区域：已完成任务列表（可收起）
- 浮动操作按钮：新建待办

### 3.4 设置页面

**布局结构**：
- 顶部导航栏：标题、返回
- 设置项分类：
  - 外观设置：主题颜色、深色模式、字体大小、屏幕方向
  - 数据管理：备份、恢复、导出数据
  - 安全设置：应用锁、生物识别
  - 关于：版本信息、隐私政策、用户协议、反馈

---

## 四、数据存储设计

### 4.1 数据库结构

**数据库名称**：`notes_database`

**表结构**：

| 表名 | 说明 |
|------|------|
| `notes` | 笔记主表 |
| `categories` | 分类表 |
| `note_images` | 笔记图片关联表 |
| `todos` | 待办任务表 |

### 4.2 数据备份

**备份格式**：JSON 文件

**备份内容**：
- 所有笔记数据
- 分类数据
- 待办任务数据
- 笔记图片（存储为文件路径）

**备份路径**：应用私有目录 / 外部存储

---

## 五、技术架构

### 5.1 架构模式

采用 MVVM（Model-View-ViewModel）架构模式：

- **Model**：数据层，包含实体类、DAO、Repository
- **View**：UI层，使用 Jetpack Compose 构建
- **ViewModel**：业务逻辑层，管理UI状态

### 5.2 核心技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| Kotlin | 1.8+ | 主要开发语言 |
| Jetpack Compose | 1.6+ | UI框架 |
| Room | 2.6+ | 本地数据库 |
| Kotlin Coroutines | 1.7+ | 异步编程 |
| Material Design 3 | 1.2+ | 设计系统 |
| Timber | 5.0+ | 日志框架 |
| Coil | 2.5+ | 图片加载 |

### 5.3 关键类说明

**UI层关键类**：
- `NotesListScreen`：笔记列表页面
- `NoteEditScreen`：笔记编辑页面
- `TodoListScreen`：待办任务页面
- `CategoriesScreen`：分类管理页面
- `SearchScreen`：搜索页面
- `SettingsScreen`：设置页面
- `StatsScreen`：统计页面
- `TrashScreen`：回收站页面

**ViewModel层关键类**：
- `NotesViewModel`：笔记业务逻辑

**数据层关键类**：
- `NoteEntity`：笔记实体
- `CategoryEntity`：分类实体
- `TodoEntity`：待办实体
- `NoteImageEntity`：图片实体
- `NotesRepository`：笔记数据仓库
- `TodoRepository`：待办数据仓库
- `AppDatabase`：数据库实例

**工具类**：
- `BackupManager`：备份管理
- `ReminderManager`：提醒管理
- `NoteExporter`：导出管理
- `OcrHelper`：OCR识别
- `SpeechToTextHelper`：语音转文字
- `ImageUtils`：图片处理
- `AppLockStore`：应用锁管理
- `BiometricHelper`：生物识别

---

## 六、安全机制

### 6.1 应用锁
- 支持密码锁和生物识别（指纹/面容）
- 应用退到后台后自动锁定
- 验证通过后才能访问应用内容

### 6.2 数据安全
- 本地数据库存储，不上传云端
- 备份文件加密存储
- 安全检测：检测应用篡改、调试模式等威胁

### 6.3 权限管理
- 运行时权限申请：相机、录音、存储、通知等
- 权限引导页面：首次使用时说明权限用途

---

## 七、特色功能

### 7.1 OCR文字识别
- 支持从图片中提取文字
- 自动插入到笔记内容中

### 7.2 语音转文字
- 实时语音识别
- 识别结果自动插入笔记

### 7.3 数据导出
- PDF导出：保留格式和图片
- 长图导出：生成PNG图片

### 7.4 桌面小部件
- 显示最近笔记
- 快捷新建笔记

### 7.5 涂鸦功能
- 内置涂鸦画板
- 支持多种颜色和画笔粗细

---

## 八、开发完成日期

本软件于 **2026年6月** 开发完成并首次发布。

---

**软件著作权人**：
**日期**：2026年7月